# Cloud Tasks

Claude Code should follow the root project instructions in `AGENTS.md`.

`AGENTS.md` is the source of truth for repository layout, package boundaries, required validation
commands, website documentation expectations, and production deployment notes.
